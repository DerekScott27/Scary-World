using UnityEngine;
using System.Collections;


//public class AttackArea : MonoBehaviour
//{
    //private int damage = 3;



    //private void OnTriggerEnter2D(Collider2D collider) //1. creates a void function called OnTriggerEnter2d

                                                       //2. parameters are calling the Collider2d class and obstantiating it into an object, naming the object collider.
    //{
        //if (collider.GetComponent<Health>() != null)  //1. if statement, collider.GetComponent Health is not equal to null aka checking if Health component is existent

                                                                                                                              //so it doesnt affect health when interacting with non-enemies
        //{
            //Health health = collider.GetComponent<Health>(); //2. then health object is = to the class health
                                                             //so it assigns the Health component obtained from the collider object to the health variable.

           // health.TakeDamage(damage);              //3. Calling the health object.Damage fucntion/method from the class health

                                                

        //}
    //}




//}
